/** Idioma de reconocimiento y de síntesis de voz. */
export const SPEECH_LANG = 'es-CO';

/** Velocidad de la voz que lee las llamadas simuladas. */
export const SPEECH_RATE = 1.05;

/** Frases que el navegador dice al anunciar una llamada entrante. */
export const CALL_PREFIX = 'Llamada entrante desde';
